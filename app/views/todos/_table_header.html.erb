<tr>
  <th>
    <%= render SortButtonComponent.new(column: 'title', direction: direction, selected: selected) %>
  </th>
  <th>
    <%= render SortButtonComponent.new(column: 'description', direction: direction, selected: selected) %>
  </th>
  <th>
    <%= render SortButtonComponent.new(column: 'status', direction: direction, selected: selected) %>
  </th>
  <th id="action" colspan="3" class="text-center">Actions</th>
</tr>
